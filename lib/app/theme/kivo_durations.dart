abstract final class KivoDurations {
  static const Duration fast = Duration(milliseconds: 140);
  static const Duration normal = Duration(milliseconds: 240);
  static const Duration slow = Duration(milliseconds: 420);
  static const Duration answerFeedback = Duration(milliseconds: 500);
  static const Duration storyTransition = Duration(milliseconds: 650);
}
