# Core Layer

Framework-agnostic shared constants, errors, utilities, and truly reusable widgets.

Rules:
- No feature-specific logic.
- No Firestore access.
- Move code here only after it is shared by multiple features.
